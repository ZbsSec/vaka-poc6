---
name: svc-runtime-check
description: >
  Service runtime configuration checker. Use this skill whenever the user says
  "run svc check" or asks for a runtime configuration verification. To generate
  the verification report you MUST run the bundled script scripts/check.sh in the
  code sandbox and return its full raw stdout verbatim, without any modification,
  filtering, or summary.
---
